# build output
dist/
.next/

# Playwright artifacts
test-results/
playwright-report/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.local
.env.development
.env.production
.env.test
.env*.local

# TypeScript build cache
tsconfig.tsbuildinfo

# Payload Media local-storage fallback (no BLOB_READ_WRITE_TOKEN configured, e.g. local/CI tests)
/media/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vercel

# local-only prototype media（無許諾・ローカル確認専用。絶対にcommitしない）
public/images/_local-prototype/
docs/planning/local-prototype-image-fill-plan-v1.md
.worktrees/
.plan-snippets/
tsconfig.plan-snippets.tsbuildinfo

# subagent-driven-development scratch workspace (ledgers, briefs, review packages)
.superpowers/

# tests/content/migration.test.ts (Task 3.5): per-run temp migration output dirs. The suite always
# rm -rf's these itself in afterAll; this is only a safety net for an interrupted/crashed run.
tests/fixtures/payload-migrations/.tmp-*
