# (retired)

このディレクトリのドキュメント（設計判断・運用方針・作業計画）は、2026-07-20の再編で `docs/decisions/` / `docs/plans/` / `docs/reference/` / `docs/archive/` の4棚へ分割・移動した。

新しい入口は `../README.md`（PMダッシュボード）。

`docs/planning/` にはコード側の型定義草案（`nextjs_data_types_v1.ts`・`content_config_draft_v1.ts`、今回のドキュメント再編の対象外）だけが残っている。
